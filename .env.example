# 「此刻」环境变量示例
# 复制为 .env 并填入真实值。.env 已被 .gitignore 排除，不会进仓库。
#
# 音乐来源 Audius 免认证，无需任何凭据。
# DeepSeek key 从本地 .env 读取，构建时注入，App 内不再让用户填写。

# ===== DeepSeek（构建期注入，从 platform.deepseek.com 申请）=====
# EXPO_PUBLIC_ 前缀的变量会被 Expo 自动注入 process.env，运行时直接读取
EXPO_PUBLIC_DEEPSEEK_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===== DeepSeek 代理（仅 Web 端用，可选）=====
# 部署 edge/deepseek-proxy 后填入代理 URL，解决 Web 端 CORS
# 移动端 RN 直连 api.deepseek.com，不走代理
EXPO_PUBLIC_DEEPSEEK_PROXY_URL=
